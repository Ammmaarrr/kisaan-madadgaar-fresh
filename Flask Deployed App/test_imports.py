print("Start imports", flush=True)
import numpy
print("Imported numpy", flush=True)
import torch
print("Imported torch", flush=True)
import PIL
print("Imported PIL", flush=True)
import CNN
print("Imported CNN", flush=True)
